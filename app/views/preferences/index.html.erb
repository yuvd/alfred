
<div class="container profile_index">

<div class="d-flex justify-content-start">
  <%= cl_image_tag @user.photo, height: 50, width: 50, crop: :fill %>
  <h1>Your Profile</h1>
</div>

  <div class="d-flex justify-content-start">
    <p>  ----  INFOS </p>
    <p><i class="fa fa-pencil-alt"></i></p>
  </div>
<p><%= current_user.name %></p>
<p><%= current_user.nickname %></p>
<p><%= current_user.location %></p>
<p>change password </p>


<div class="container">
  <p class="mt-5"> ---- PREFERENCES </p>
  <div class="row">
    <div class="col-xs-12">
        <% @preferences.each do | preference | %>
        </div>
        <div class="row ml-3">
          <div class="col-xs-12">
          <%=  preference.category.name %>
          </div>
        </div>
        <% end %>
  </div>
</div>
