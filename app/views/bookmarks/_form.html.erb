<%= simple_form_for [@place, @bookmark] do |f| %>
<%= f.input :time, as: :string, label: false, placeholder: "pick a date and time", required: true, input_html: {class: "datepicker"} %>  <%= f.submit "Confirm", class: "btn btn-primary box_btn" %>
<% end %>
