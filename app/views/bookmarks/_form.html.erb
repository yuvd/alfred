<%= simple_form_for [@place, @bookmark] do |f| %>
<%= f.input :time, as: :string, required: false, input_html: {class: "datepicker"} %>  <%= f.submit "Add to schedule", class: "btn btn-primary" %>
<% end %>
