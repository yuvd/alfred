<%= simple_form_for [@place, @bookmark] do |f| %>
<%= f.input :time, as: :string, label: false, placeholder: "pick a date and time", required: true, input_html: {class: "datepicker"} %>  <%= f.submit "Add to schedule", class: "btn btn-primary" %>
<% end %>
